@@@@@@@@@@@@@@@@@@
Bash Aliases Hints
@@@@@@@@@@@@@@@@@@
.. Okay, just one.

Hint: To run the native command and not the alias,
      use a ``\`` prefix, e.g., ::

        \rm

      will call the real ``/bin/rm`` and not the alias.

